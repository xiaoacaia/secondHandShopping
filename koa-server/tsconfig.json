{
  "compilerOptions": {
    "outDir": "dist",
    // 目标语言的版本 async 
    "target": "ES2015",
    // 生成代码的模板标准 Dynamic imports: await import
    "module": "commonjs", 
    // 允许 export= 导出, import from 导入
    "esModuleInterop": true,
    // 默认所有可见的"@types"包会在编译过程中被包含进来
    // 只有被列出来的包才会被包含进来
    "types": [
      "node"
    ],
    // 只有typeRoots下面的包才会被包含进来
    "typeRoots": [
      "./node_modules/@types"
    ],
  },
  "exclude": [
    "node_modules",
    "./dist"
  ]
}